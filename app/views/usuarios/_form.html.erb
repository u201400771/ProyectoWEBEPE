<div id="menu"></menu>

<%= simple_form_for @usuario, :html => { :class => 'form-horizontal' } do |f| %>


  <% if @usuario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usuario.errors.count, "error") %> prohibited this usuario from being saved:</h2>

      <ul>
      <% @usuario.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.input :codigo %>
  <%= error_span(@usuario[:codigo]) %>

  <%= f.input :apellido_paterno %>
  <%= error_span(@usuario[:apellido_paterno]) %>

  <%= f.input :apellido_materno %>
  <%= error_span(@usuario[:apellido_materno]) %>

  <%= f.input :primer_nombre %>
  <%= error_span(@usuario[:primer_nombre]) %>

  <%= f.input :segundo_nombre %>
  <%= error_span(@usuario[:segundo_nombre]) %>

  <%= f.input :cod_rol %>
  <%= error_span(@usuario[:cod_rol]) %>

  <!--
  <div class="form-group">
  <%= f.label :empresa_id %>
  <%= f.collection_select :empresa_id, Empresa.all, :id, :name, {prompt: "Select a Empresa"}, {class: "form-control"} %>
  <%= error_span(@usuario[:empresa_id]) %>
  </div>
  --!>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
