<!--<h1>Editar Usuario</h1>

<%#= render 'form' %>

<%#= link_to 'Mostrar', @usuario %> 
<%#= link_to 'Atrás', usuarios_path %>

<%#= link_to t('.Mostrar', :default => t("helpers.links.show")),
            @usuario,
            :class => 'btn btn-primary' %>

<%#= link_to t('.Atrás', :default => t("helpers.links.back")),
            new_usuario_path,
            :class => 'btn btn-primary' %>

<div class="btn-group" role="group" aria-label="...">
  <button type="button" class="btn btn-default"><%#= link_to 'Mostrar', @usuario %> </button>
  <button type="button" class="btn btn-default"><%#= link_to 'Atrás', usuarios_path %></button>
</div>
--!>


<%#= render 'form' %>

<%- model_class = Usuario -%>
<div class="page-header">
  <h1><%=t '.title', :default => [:'helpers.titles.edit', 'Editar %{model}'], :model => model_class.model_name.human.titleize %></h1>
</div>
<%= render :partial => 'form' %>
