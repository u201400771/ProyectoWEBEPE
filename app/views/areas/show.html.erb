<%- model_class = Area -%>
<div class="page-header">
  <h1>Editar Unidad de Negocio</h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:name) %>:</strong></dt>
  <dd><%= @area.name %></dd>
</dl>

<div class="btn-group" role="group" aria-label="...">
  <button type="button" class="btn btn-default"><%= link_to 'Editar', edit_area_path(@area) %> </button>
  <button type="button" class="btn btn-default"><%= link_to 'Atrás', areas_path %></button>
</div>

<%#= link_to t('.back', :default => t("helpers.links.back")),
              areas_path, :class => 'btn btn-default'  %>
<%#= link_to t('.edit', :default => t("helpers.links.edit")), edit_area_path(@area), :class => 'btn btn-default' %>
<%#= link_to t('.destroy', :default => t("helpers.links.destroy")),
              area_path(@area),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
